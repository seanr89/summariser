name: Build Windows

on:
  push:
    branches: [ main, electron ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '20'

    - name: Install dependencies
      run: npm install
      working-directory: ./ai-content-summarizer

    - name: Build Windows installer
      run: npm run dist:win
      working-directory: ./ai-content-summarizer

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: ai-content-summarizer-windows-x64
        path: ./ai-content-summarizer/dist/ai-content-summarizer Setup*.exe
