name: Build Mac

on:
  push:
    branches: [ main, electron ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v3

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '20'

    - name: Install dependencies
      run: npm install
      working-directory: ./ai-content-summarizer

    - name: Build macOS app
      run: npm run dist:mac
      working-directory: ./ai-content-summarizer

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: ai-summarizer
        path: ./ai-content-summarizer/dist_electron/summarizer-*.dmg